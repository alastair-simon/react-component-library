{
  "name": "react-component-library",
  "version": "0.0.0",
  "private": true,
  "description": "React component library monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8"
  }
}
